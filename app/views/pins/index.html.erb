<% @pins.each do |pin| %>
  <div class="">
    <%= link_to (image_tag pin.asset.url), pin_path(pin) %>
  </div>
  <div class="">
    <%= pin.title %>
  </div>
  <div class="">
    Posted by <%= pin.user.email %>
  </div>
<% end %>
